# Sheesh-rs
- TUI for managing and accessing ssh connections
- Should be able to
  - CRUD SSH connections
  - open ssh connection on the terminal
  - open ssh connection as a tab on the system itself
    - When ssh connection is active should show stats usage